apiVersion: v1
kind: Service
metadata:
  name: quiz-app-service
  namespace: quiz-app-ns
spec:
  type: NodePort
  selector:
    app: quiz-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31873
